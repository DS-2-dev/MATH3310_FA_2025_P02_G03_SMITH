Place Python scripts and modules here.

Suggested layout:

- `src/<package>/` for library code
- `src/scripts/` for CLI entrypoints

Example: move an existing `mp2.py` to `src/scripts/mp2.py` and expose a console entry in `setup.cfg` or `pyproject.toml`.
